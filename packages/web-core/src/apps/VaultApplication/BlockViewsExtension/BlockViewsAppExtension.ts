import { injectable } from 'inversify';
import { BaseExtension } from '../../../framework/BaseExtension';

@injectable()
export class BlockViewsAppExtension extends BaseExtension {
  async register() {}
}
