overwrite: true
schema: "http://localhost:3333/api/graphql"
documents: "./**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-query
    config:
      fetcher: 
        endpoint: '`${import.meta.env.SNOWPACK_PUBLIC_API_URL}/api/graphql`'
        fetchParams:
          headers:
            Content-Type: 'application/json'
          credentials: 'include'

